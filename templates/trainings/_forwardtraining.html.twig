<div class="container">
    <div class="col">
        <h2>La formation primordiale: {{forwardtraining.name}}</h2>
        {% for image in forwardtraining.images %}
            <div>                
                <img class="card-img-bottom" src="{{ asset('/uploads/'~image.image) }}" alt="image de la formation" width="200px" height="400px" > 
            </div>
        {% endfor %}
        
    </div>
</div>
