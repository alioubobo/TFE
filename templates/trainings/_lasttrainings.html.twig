{% block body %}
<div class="album py-5 ">
    <div class="container">
     <h2 class" ">Les formations  recentes</h2>
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4 g-3">
        {% for lasttraining in lasttrainings %}
            <div class="col">            
                <div class="card shadow-sm">                    
                    <a href={{path('show_training', {id:lasttraining.id})}}>photo de la formation</a>
                </div>            
            </div>
        {% endfor %}
        </div>
    </div>
</div> 
{% endblock %}